version: '3'
services:
  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
      tags:
        - doguhannilt/mernappetite-frontend:latest
    ports:
      - "3000:3000"
    depends_on:
      - backend
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
      tags:
        - doguhannilt/mernappetite-backend:latest
    ports:
      - "5000:5000"
